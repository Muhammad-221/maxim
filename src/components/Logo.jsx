import { NavLink } from "react-router-dom";

export default function PageLogo(){
    return(
        <div className="title">
            <NavLink to="#">
                <h1>MAXIM</h1>
            </NavLink>
        </div>
    )
}